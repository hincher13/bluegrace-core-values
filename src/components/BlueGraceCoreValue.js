const BlueGraceCoreValue = (props) => {
    return(
        <div>
            <p>This is where my image will go</p>
            <p>{props.coreValue}</p>
        </div>
    )
};

export default BlueGraceCoreValue;